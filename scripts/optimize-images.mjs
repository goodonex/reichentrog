#!/usr/bin/env node
/**
 * Platzhalter: hier später sharp-Pipeline (WebP/AVIF) anbinden.
 * Nutzung: npm run optimize:images
 */
console.log('[optimize:images] Noch keine Quellbilder konfiguriert — Pipeline vorbereitet.');
